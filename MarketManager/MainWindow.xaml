<Window x:Class="MarketManager.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:local="clr-namespace:MarketManager.ViewModel"
                      Title="MainWindow"
                      Height="600"
                      Width="600"
                      Background="WhiteSmoke"
                      BorderThickness="0" 
                      ResizeMode="CanResizeWithGrip"
                      WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <TextBox  Text="{Binding SearchTerm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10,26,275,518.5" SpellCheck.IsEnabled="True"/>
        <DataGrid Margin="10,78,51,108.5" AutoGenerateColumns="True"  BorderThickness="1" ItemsSource="{Binding QueryResult.Lookup, Mode=OneWay}">
        </DataGrid>
    </Grid>


</Window>
    
    
